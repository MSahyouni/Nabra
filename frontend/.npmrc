# OneDrive ACLs can prevent pnpm's isolated linker from creating transitive junctions.
# The hoisted linker keeps multiple versions nested correctly without relying on them.
node-linker=hoisted
confirm-modules-purge=false
